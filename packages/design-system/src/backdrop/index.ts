// The bundler resolves `./backdrop` to backdrop.web.tsx (Vite) or
// backdrop.native.tsx (Metro) by extension. Types come from the
// platform-agnostic props module.
export { Backdrop } from './backdrop';
export type { BackdropOwnProps } from './backdrop.props';
